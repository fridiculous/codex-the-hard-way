# Nested Fixture Instructions

This file exists only for deterministic nested Codex prompt-compilation tests.

Sentinel: NESTED_AGENT_SENTINEL

When working in this nested fixture directory, the model-visible prompt input should include `NESTED_AGENT_SENTINEL`.
