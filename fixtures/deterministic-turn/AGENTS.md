# Root Fixture Instructions

This file exists only for deterministic Codex prompt-compilation tests.

Sentinel: ROOT_AGENT_SENTINEL

When working in this fixture root, the model-visible prompt input should include `ROOT_AGENT_SENTINEL`.
